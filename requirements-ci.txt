# Human-edited input for the quality-gate environment; GPU/model runtimes stay in
# requirements.txt and are exercised by provider-contract tests with fakes.
#
# `requirements-ci.lock.txt` is the compiled pin set that CI installs on the 3.10
# baseline. Recompile it whenever this file changes:
#
#   python -m piptools compile requirements-ci.txt \
#       --output-file requirements-ci.lock.txt
anthropic>=0.40.0
pydantic-settings>=2.15.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.1
aiohttp>=3.10.0
mutagen>=1.48.1
librosa>=0.11.0
filelock>=3.16.0
openai>=1.0.0
edge-tts>=7.2.0
Pillow>=12.3.0
numpy>=1.26.0
google-api-python-client>=2.118.0
google-auth-oauthlib>=1.2.0
# server.py is imported by the security contract tests. Keep its lightweight
# web/runtime dependencies in CI even though GPU/model packages stay excluded.
fastapi>=0.100.0
python-multipart>=0.0.6
jinja2>=3.1.0
# Static analysis. Ruff and the scoped mypy gate both block; the wider mypy run
# is advisory until the remaining debt in core/application and infrastructure is
# cleared (see docs/CODE_REVIEW.md).
ruff>=0.16.0
mypy>=1.11.0
